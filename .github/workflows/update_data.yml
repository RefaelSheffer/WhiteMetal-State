name: Update SLV Dashboard Data

on:
  workflow_dispatch:
  schedule:
    # ארבע פעמים ביום ב-06:30, 12:30, 18:30 ו-22:30 UTC (אחרי סגירת שוק ארה"ב)
    - cron: "30 6,12,18,22 * * *"

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pipeline
        run: |
          python tools/fetch_cot.py --start-year 2015
          python -m engine.main --refresh

      - name: Commit & push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data
          if git diff --cached --quiet; then
            echo "No changes in public/data."
          else
            git commit -m "chore: update dashboard data"
            git push
          fi
